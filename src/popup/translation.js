// Fonctions liées à la traduction

import { translateText as helpersTranslateText } from '../utils/helpers.js';

// Réexport pour compatibilité avec le reste du code
export const translateText = helpersTranslateText;
